repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.15.4
  hooks:
    # Run the linter.
    - id: ruff-check
      files: ^sdk/py/
    # Run the formatter.
    - id: ruff-format
      files: ^sdk/py/